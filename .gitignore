# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment
.env
.env.*
!.env.example

# Local Netlify folder
.netlify

# Supabase local CLI state
supabase/.temp

.worktrees/

# Saídas das ferramentas de auditoria de layout (scripts .mjs ficam versionados)
auditoria-atividades/
pdf-relatorio-p*.png
pdf-habilidades-*.png

# saida das auditorias de responsividade (gerada, nao versionada)
auditoria-telas/
auditoria-atividades/
auditoria-alcance/

# Scripts de OUTRO cliente (TI Verde) que precisam morar aqui porque o playwright
# está instalado neste projeto e a resolução ESM sobe a partir do ARQUIVO.
# Ver scripts-tiverde/LEIA-ME.md. Ignorados para que um `git add .` distraído não
# leve script de cliente para dentro do repositório do produto.
scripts-tiverde/

# saída de renderização das ferramentas de conferência visual
scripts/saida/
